The OpenCV will be included into the build process as external project. During the build process 
CMake will download a local copy of the OpenCV based on the version number, which is defined at 
the top level CMakeLists.txt.